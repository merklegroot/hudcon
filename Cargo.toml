[package]
name = "hudcon"
version = "0.1.0"
edition = "2021"
description = "Simple CLI for basic CPU information"

[dependencies]
crossterm = { version = "0.29.0", default-features = false, features = ["events"] }
sysinfo = "0.33"
