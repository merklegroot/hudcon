{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug hudcon",
      "program": "${workspaceFolder}/target/debug/hudcon",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo: build (debug)"
    }
  ]
}
